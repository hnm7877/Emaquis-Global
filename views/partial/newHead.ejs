<style>
  /*------ Settings ------*/
  .containerBell {
    --color: #a5a5b0;
    --size: 30px;
    display: flex;
    justify-content: center;
    align-items: center;
    position: relative;
    cursor: pointer;
    font-size: var(--size);
    user-select: none;
    fill: var(--color);
  }

  .containerBell .bell-regular {
    position: relative;
    animation: keyframes-fill 0.5s;
  }

  .containerBell .bell-solid {
    /* ------position: absolute;------ */
    display: none;
    animation: keyframes-fill 0.5s;
  }

  /* ------ On check event ------ */
  .containerBell input:checked ~ .bell-regular {
    display: none;
  }

  .containerBell input:checked ~ .bell-solid {
    display: block;
  }

  /* ------ Hide the default checkbox ------ */
  .containerBell input {
    position: absolute;
    opacity: 0;
    cursor: pointer;
    height: 0;
    width: 0;
  }

  /* ------ Animation ------ */
  @keyframes keyframes-fill {
    0% {
      opacity: 0;
    }

    25% {
      transform: rotate(25deg);
    }

    50% {
      transform: rotate(-20deg) scale(1.2);
    }

    75% {
      transform: rotate(15deg);
    }
  }

  #countNotyf {
    position: relative;
    top: -7px;
    right: 14px;
    background: red;
    color: white;
    width: 15px;
    height: 15px;
    border-radius: 50%;
    border: solid 1.2px white;
    z-index: 1;
  }

  .main-navbar-pro {
    background: linear-gradient(90deg, #3498db 0%, #2c3e50 100%);
    color: #fff;
    box-shadow: 0 4px 18px rgba(52, 152, 219, 0.13);
    border-radius: 0 0 18px 18px;
    padding: 0.7rem 2.2rem 0.7rem 1.2rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-wrap: wrap;
    min-height: 64px;
    position: relative;
    z-index: 10;
  }
  .main-navbar-pro .navbar-left {
    display: flex;
    align-items: center;
    gap: 1.2rem;
  }
  .main-navbar-pro .navbar-icon {
    font-size: 2.1rem;
    color: #fff;
    background: rgba(255, 255, 255, 0.13);
    border-radius: 50%;
    padding: 0.5rem;
    margin-right: 0.7rem;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 2px 8px rgba(44, 62, 80, 0.1);
  }
  .main-navbar-pro .navbar-title {
    font-size: 1.45rem;
    font-weight: 700;
    letter-spacing: 0.5px;
    margin: 0;
    color: #fff;
  }
  .main-navbar-pro .navbar-user {
    display: flex;
    align-items: center;
    gap: 0.7rem;
    font-size: 1.08rem;
    font-weight: 500;
  }
  .main-navbar-pro .navbar-user .user-role {
    font-size: 0.98rem;
    color: #e0eaf6;
    margin-left: 0.5rem;
    font-weight: 400;
  }
  .navbar-user button span,
  .navbar-user .user-role {
    color: #fff !important;
    font-size: 1.08rem;
    font-weight: 500;
    display: inline;
    white-space: nowrap;
  }
  @media (max-width: 700px) {
    .main-navbar-pro {
      flex-direction: column;
      align-items: flex-start;
      padding: 0.7rem 0.7rem 0.7rem 0.7rem;
      min-height: unset;
    }
    .main-navbar-pro .navbar-title {
      font-size: 1.1rem;
    }
    .main-navbar-pro .navbar-user {
      font-size: 0.98rem;
    }
  }
</style>
<script>
  document.addEventListener("DOMContentLoaded", function () {
    var btn = document.getElementById("page-header-user-dropdown");
    var menu = btn && btn.nextElementSibling;
    if (btn && menu) {
      btn.addEventListener("click", function (e) {
        e.stopPropagation();
        menu.classList.toggle("show");
      });
      document.addEventListener("click", function () {
        menu.classList.remove("show");
      });
    }
  });
</script>
<style>
  .dropdown-menu {
    display: none;
  }
  .dropdown-menu.show {
    display: block;
  }
</style>
<!-- Begin page -->
<div id="layout-wrapper">
  <header id="page-topbar">
    <nav class="main-navbar-pro">
      <div class="navbar-left">
        <span class="navbar-icon"><i class="fas fa-utensils"></i></span>
        <span class="navbar-title">E-MAQUIS</span>
      </div>
      <div class="navbar-user dropdown d-inline-block ml-2">
        <button
          type="button"
          class="btn header-item waves-effect d-flex align-items-center"
          id="page-header-user-dropdown"
          data-toggle="dropdown"
          aria-haspopup="true"
          aria-expanded="false"
          style="background: none; border: none; box-shadow: none; padding: 0"
        >
          <span
            ><%= locals.user?.nom %> <%= locals.user?.prenom ||
            locals.user?.prenoms %></span
          >
          <span class="user-role"><%= locals.user?.role %></span>
          <i
            class="fas fa-cog"
            style="margin-left: 10px; font-size: 1.3em; color: #fff"
          ></i>
        </button>
        <div class="dropdown-menu dropdown-menu-right">
          <a
            class="dropdown-item d-flex align-items-center justify-content-between"
            href="/profile"
          >
            <span>Profil</span>
          </a>
          <a
            class="dropdown-item d-flex align-items-center justify-content-between"
            href="/deconnexion"
          >
            <span>Déconnexion</span>
          </a>
        </div>
      </div>
    </nav>
  </header>

  <!-- Modal -->
  <div
    class="modal fade"
    id="exampleModal"
    tabindex="-1"
    aria-labelledby="exampleModalLabel"
    aria-hidden="true"
  >
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h1 class="modal-title fs-5" id="exampleModalLabel">Modal title</h1>
          <button
            type="button"
            class="btn-close"
            data-bs-dismiss="modal"
            aria-label="Close"
          ></button>
        </div>
        <div class="modal-body modal-body-notyf">...</div>
        <div class="modal-footer">
          <button
            type="button"
            class="btn btn-secondary"
            data-bs-dismiss="modal"
          >
            Fermer
          </button>
          <button type="button" class="btn btn-primary">
            <a
              href="/listeproduit"
              class="text-wrap text-decoration-none text-white"
              >Ajouter des boissons</a
            >
          </button>
        </div>
      </div>
    </div>
  </div>
</div>
